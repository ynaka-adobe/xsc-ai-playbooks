# sync-da-content screenshots

Drop the image referenced by `../SKILL.md` here:

- **`da-config-permissions.png`** — a target org's `da.live/config` → **`permissions`** sheet granting an identity
  `write` on `CONFIG` and content (`/ + **`). Save it with exactly this name so the `![…](assets/da-config-permissions.png)`
  reference resolves.
